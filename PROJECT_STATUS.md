# Perler Beads Studio 项目状态说明

更新日期：2026-06-11

本文用于记录当前已经完成的能力、仍未完成的事项，以及建议的后续开发顺序。

## 已完工

### 工程与基础架构

- 已完成 Electron + Vue 3 + TypeScript + Tailwind CSS 工程搭建。
- 已接入 `@iconify/vue`，使用 RemixIcon 图标。
- 已配置 electron-vite 构建流程。
- 已支持本地 Web 预览和 Electron 桌面端两种开发形态。
- 已生成并维护 `AGENTS.md`，记录项目约定、架构规则、验证流程和安全边界。

### 主题与工作台界面

- 已完成桌面工作台主界面。
- 已支持系统、昼、夜三种主题模式。
- 已支持快速主题切换。
- 已按导入原图、AI 优化、生成图纸、手工精修、导出分享的工作流组织界面。
- 已提供左侧设置区、中间图纸预览区、右侧 AI 平台区。

### 厂商色卡

- 已将厂商色卡拆分为独立配置文件，位置为 `src/renderer/src/data/palettes/`。
- 默认色卡为 `MARD 221`。
- 已对接以下 Bitbead 色卡数据：
  - `MARD 221`：221 色
  - `MARD 291`：291 色
  - `Perler`：117 色
  - `Hama`：89 色
  - `Artkal`：176 色
  - `Artkal Mini`：221 色
- 图纸生成、当前用色、CSV 清单和 PNG 导出均会使用当前选中的厂商色卡。

### 图片转拼豆图纸

- 已支持导入图片并转换为拼豆网格。
- 已支持 32 x 32、48 x 48、64 x 64、96 x 96 画板规格。
- 已支持最大颜色数控制。
- 已支持 Floyd-Steinberg 抖色开关。
- 已将透明像素识别为空格/无珠格，透明底图片不会再被匹配成黑色背景。
- 已将半透明像素按白底合成后再做色卡匹配，减少透明边缘黑边。
- 已实现基于当前图片内容的色卡筛选，不再简单取厂商色卡前 N 个颜色。

### 图纸预览与编辑

- 预览区格子已改为方形。
- 已支持行列标号显示和隐藏。
- 已支持画笔、填充、吸管、橡皮工具。
- 橡皮工具已改为清空格子，而不是涂成白色。
- 已支持撤销和重做。
- 已支持当前用色统计和色块选择。
- 已支持完整用珠清单展示。

### AI 接入

- 已采用 Base URL + API Key + 模型拉取/选择的接入方式。
- 已支持 OpenAI-compatible `/models` 模型列表拉取。
- Electron 桌面端 AI 请求通过主进程 IPC 转发。
- 本地 Web 预览在 `localhost` / `127.0.0.1` 下允许浏览器直连 AI，方便开发调试。
- 已支持 AI 图片优化：导入图片后，可调用配置的图片编辑模型生成参考图，再进入本地确定性拼豆转换流程。
- API Key 当前仅保存在运行时内存中，不写入项目文件和导出文件。

### 项目保存与打开

- 已支持 `.pbd.json` 项目保存。
- 已支持 `.pbd.json` 项目打开。
- 保存内容包含画板设置、厂商色卡 ID、当前网格、来源名和 AI 供应商元数据。
- 项目文件不保存 API Key。
- Web 预览下有下载/上传 JSON 的兜底行为。
- Electron 桌面端走原生保存/打开文件对话框。

### 导出

- 已支持 PNG 打印图纸导出。
- PNG 图纸包含：
  - 四周坐标栏
  - 每格细网格线
  - 每 5 格加粗网格线
  - 有珠格内显示厂商色号
  - 底部色卡统计
  - 色块、色号、数量
- PNG 导出不添加水印。
- 已支持 CSV 用珠清单导出。

## 未完工

### 安全与配置

- API Key 还没有接入系统级安全存储。
- AI 供应商配置档案尚未持久化。
- 模型能力标签尚未持久化。
- 还没有区分开发直连和生产发布策略的配置开关。

### AI 能力

- 尚未支持文本生成图片。
- 尚未支持可编辑 AI Prompt 模板。
- 尚未支持 AI 结果历史记录。
- 尚未支持多模型能力识别和过滤。
- 尚未支持后端代理、额度、审计和商业化计费链路。

### 图纸算法

- 当前颜色匹配仍是 RGB 距离，尚未接入 Lab / CIEDE2000 感知色差。
- 尚未支持孤立像素清理、区域平滑、边缘修整等后处理。
- 尚未支持按用户已有库存限制色卡。
- 尚未支持透明/无珠区域的高级编辑和批量替换。

### 编辑器

- 尚未支持拖拽连续绘制。
- 尚未支持框选、移动、复制、粘贴。
- 尚未支持区域替换颜色。
- 尚未支持镜像、旋转、裁剪。
- 尚未支持缩放和平移。
- 尚未支持键盘快捷键。
- 大尺寸图纸仍使用 DOM 按钮渲染，后续应迁移到 Canvas 或 WebGL。

### 导出与打印

- 尚未支持 PDF 导出。
- 尚未支持分页打印和拼接打印。
- 尚未支持低色打印用符号图。
- 尚未支持独立的可打印用珠统计表。
- 尚未支持完整项目包 `.pbd`，目前仍是 JSON 项目文件。

### 桌面发布

- 尚未配置应用图标、安装包和自动更新。
- 尚未配置 Windows/macOS/Linux 打包产物。
- 尚未配置发布渠道和版本更新说明。
- 尚未配置 GitHub Actions CI/CD。

### 测试与质量

- 尚未建立单元测试。
- 尚未建立图像转换算法测试样例。
- 尚未建立导出图片视觉回归测试。
- 尚未建立端到端自动化测试。

## 建议后续优先级

1. 做 AI 供应商配置档案，并接入安全的 API Key 存储。
2. 做 Canvas 渲染器，提升大图纸预览和编辑性能。
3. 做 PDF / 分页打印导出。
4. 做 Lab / CIEDE2000 色彩匹配和图纸后处理。
5. 做完整 `.pbd` 项目包，包含源图、AI 参考图、预览图和项目 JSON。
6. 做桌面安装包和 GitHub Actions 自动构建。

## 当前验证命令

常规开发验证：

```powershell
npm run typecheck
npm run build
```

本地启动：

```powershell
npm run dev
```
